extern char* gets(char* s);
extern char* puts(char* s);
extern int printf(char *s);

int main()
{
    int a[100];
    // char b[100];
    a[10] = 10;
    printf("lalalla %d\ncaoni\nmade\nlal\nssss\nsss", a[10]);
    // gets(b);
    // puts(b);
    return 0;
}
